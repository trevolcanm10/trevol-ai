import React from "react"; // importando el hook useState
import ReactDOM from "react-dom/client"; // importando el hook useState
import "./index.css"; // importando el hook useState
import App from "./App"; // importando el hook useState

const root = ReactDOM.createRoot(document.getElementById("root")); // importando el hook useState
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);